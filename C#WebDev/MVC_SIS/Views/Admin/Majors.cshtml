@model List<Exercises.Models.Data.Major>

@{
    ViewBag.Title = "Majors";
}

<div class="row">
    <div class="col-xs-12">
        <h2>Majors</h2>
        @Html.ActionLink("Add Major", "AddMajor", "Admin", null, new { @class="btn btn-primary"})
    </div>
</div>
<div class="row">
    <div class="col-xs-12">
@*        <div><img height="400px" width="450px" src="https://2ch.hk/b/arch/2016-03-25/src/121228585/14589003617960.jpg"/></div>*@
        <table class="table table-striped" style="width: auto;">
            <thead>
            <tr>
                <th>Major</th>
                <th></th>
            </tr>
            </thead>
            <tbody>
            @foreach (var major in Model)
            {
                <tr>
                    <td>@major.MajorName</td>
                    <td>
                        @Html.ActionLink("Edit", "EditMajor", "Admin", new {id = major.MajorId}, null) |
                        @Html.ActionLink("Delete", "DeleteMajor", "Admin", new {id = major.MajorId}, null)
                    </td>
                </tr>
            }
            </tbody>
        </table>
    </div>
</div>

